require "spec_helper"

describe WonderfulSimpleForms do
  it "has a version number" do
    expect(WonderfulSimpleForms::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
